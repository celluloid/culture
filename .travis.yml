language: ruby
install: gem install bundler
script: "bundle && bundle exec rake"

rvm:
  - 2.2.2
  - ruby-head
  - jruby-head
  - rbx-3.33

matrix:
  fast_finish: true
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head
    - rvm: rbx-3.33

notifications:
  irc: "irc.freenode.org#celluloid"

sudo: false
